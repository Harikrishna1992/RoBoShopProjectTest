#Creating Variables

- name: printing the machine name for ansible playbook
  host: all
  tasks:
  ansible.builtin.debug:
   msg: print machine names {{ id }}

- name: creating variable for the asible playbook
  host: all
  task:
  vars:
   URL: gmail.google.com
  ansible.builtin.debug:
   msg: "{{ URL }}"  